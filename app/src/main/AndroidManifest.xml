<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ishwar_arcore.explorebuddy">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExploreBuddy">

        <activity android:name=".views.activities.users.UsersActivity">


        </activity>

        <activity android:name=".views.activities.chat.ChatActivity"/>
        <activity android:name=".views.activities.splash.SplashActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".views.activities.signup.SignUpActivity" />
        <activity android:name=".views.activities.details.DetailActivity"></activity>

        <activity android:name=".views.activities.makegroup.MakeGroupActivity">

        </activity>

        <activity android:name=".views.activities.home.HomeActivity" />
        <activity android:name=".views.activities.signin.SignInActivity" />
    </application>

</manifest>